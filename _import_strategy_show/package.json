{
  "private": true,
  "scripts": {
    "build:vendor": "esbuild vendor-three-entry.js --bundle --minify --format=iife --global-name=THREE --outfile=assets/vendor/three.min.js"
  },
  "dependencies": {
    "echarts": "^6.0.0",
    "esbuild": "^0.27.4",
    "three": "^0.183.2"
  }
}
