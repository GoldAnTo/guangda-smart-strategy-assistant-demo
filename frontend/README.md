# Guangda Strategy Demo Frontend

## Current status
- Vue 3 + Vite skeleton is ready
- Router is ready
- Pinia stores are ready
- Core services are ready
- Core components are ready
- Home / Demand / Recommendation / Chat views are ready

## Env
- Copy `.env.example` to `.env` if needed
- Default backend URL: `http://127.0.0.1:3001`

## Next steps
1. Run `npm install`
2. Start with `npm run dev`
3. Set `VITE_API_BASE_URL` if needed
4. Start backend server and begin联调
